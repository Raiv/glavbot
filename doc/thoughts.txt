rtt_max - верхний предел
rtt_min - нижний предел
rtt_mid - середина

rtt_cur0 - измеренное на предыдущем шаге значение
rtt_cur1 - измеренное на текущем шаге значение

Задача - держать rtt в середине допустимого коридора

1. если значение rtt_current0 выходит за пределы [rtt_min, rtt_max], то скорретировать до rtt_mid. Не предпринимать корректировок до тех пор, пока значение снова не выйдет за диапазон.
2. определение величины корректирующего воздействия
    corr_rtt       = (rtt_cur0 - rtt_mid)
    corr_par[x]    = (par[x]_curr - par[x]_min|par[x]_max)
    corr_par_steps = corr_par[x]/par[x]_step
    
3. Если произошло резкое изменение rtt, надо каким-то образом это обрабатывать (что такое резкое? Наверно имеет смысл смотреть относительно (rtt_max-rtt_mid)....)
4. Если комбинацией параметров не удалось уложиться в диапазон - дропать фреймы
    
    
p1 = { .id=GVB_CAMERA_CONTROL_BITRATE           , .weight=80, weight_cnt=0, .min=25000, max=25000000,   step=25000, deflt=10000000  }
p2 = { .id=GVB_CAMERA_CONTROL_FRAME_RATE        , .weight=10, weight_cnt=0, .min=1,     max=60,         step=1,     deflt=30        }
p3 = { .id=GVB_CAMERA_CONTROL_EXPOSURE_TIME     , .weight=5,  weight_cnt=0, .min=1,     max=10000,      step=1,     deflt=1000      }
p4 = { .id=GVB_CAMERA_CONTROL_INTER_FRAME_PERIOD, .weight=5,  weight_cnt=0, .min=0,     max=2147483647, step=1,     deflt=60        }


как увязать rtt с объёмом передаваемого буфера?

rtt = time_to + time_from
N = сколько MTU будет занимать буфер
чтобы доставить этот буфер понадобится N*rtt времени
у нас также есть статистика по камере, сколько байт она генерирует

для вычисления величины корректировки параметров нужно выяснить, насколько статистика камеры не соответствует полученному rtt. То есть при текущем rtt, мы не сможем передать тот объём, который генерирует камера. То есть, всё, что нам нужно - это следить за тем, чтобы объём генерируемых данных по камере соответствовал rtt

На чём остановился: 
	gvb-server2client.c: camera_slow_down 
	необходимо дописать код уменьшения/увеличения значения параметра, учитывая multiplier

